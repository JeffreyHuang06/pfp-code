import { testCase } from "./problemType"

let problems: Map<string, testCase[]> = new Map();

problems.set("Test", [
    {
        input: "1",
        output: "1"
    }
    ,{
        input: "1",
        output: "2"
    }
])

export default problems